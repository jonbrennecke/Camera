require 'json'

package = JSON.parse(File.read(File.join(__dir__, '../package.json')))
version = package['version']

Pod::Spec.new do |s|
  s.name = 'Camera'
  s.version = version
  s.homepage = 'https://github.com/jonbrennecke/animated-text-app'
  s.author = 'Jon Brennecke'
  s.platforms = { :ios => '12.0' }
  s.source = { :git => 'https://github.com/jonbrennecke/animated-text-app.git', :tag => "v#{version}" }
  s.cocoapods_version = '>= 1.2.0'
  s.license = 'MIT'
  s.summary = 'iOS native library of photo and video library utilities'
  s.swift_versions = '5'

  s.dependency 'Camera-Core/Camera'
  s.dependency 'Camera-Core/CameraBridge'

  # s.subspec 'Camera' do |ss|
  #   ss.source_files = 'Source/**/*.{swift,h,m}'
  #   ss.dependency 'ImageUtils', '0.0.5'
  #   ss.dependency 'VideoEffects', '0.0.27'
  # end

  # s.subspec 'CameraBridge' do |ss|
  #   ss.source_files = 'CameraBridge/**/*.{swift,h,m}'
  #   ss.dependency 'ImageUtils', '0.0.5'
  #   ss.dependency 'VideoEffects', '0.0.27'
  #   ss.dependency 'Camera/Camera', version
  # end

end
